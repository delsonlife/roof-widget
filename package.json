{
  "name": "roof-widget",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && vercel --prod"
  },
  "devDependencies": {
    "javascript-obfuscator": "^4.1.0"
  }
}
